<Page
    x:Class="PIA_PrograLocal.Views.HistorialWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:PIA_PrograLocal.Models">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <StackPanel Orientation="Horizontal" Background="#2D2D30" Padding="10" HorizontalAlignment="Stretch">
            <TextBlock Text="Historial de compras"
                       Foreground="White"
                       FontSize="22"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
            <Button x:ConnectionId='10' Content="Regresar a la tienda"
                                             
                    Background="#0078D7"
                    Foreground="White"
                    FontWeight="Bold"
                    Padding="10,5"
                    Margin="20,0,0,0"
                    HorizontalAlignment="Right"/>
        </StackPanel>

        <!-- Contenido -->
        <ScrollViewer Grid.Row="1" Margin="10,20,10,0">
            <ListView x:ConnectionId='2' x:Name="ListViewHistorial">
                <ListView.ItemTemplate>
                    <DataTemplate                                    >
                        <Border x:ConnectionId='4' BorderThickness="1"
                                BorderBrush="#CCC"
                                CornerRadius="5"
                                Margin="0,0,0,10"
                                Padding="10"
                                Background="Transparent">
                            <StackPanel>
                                <TextBlock x:ConnectionId='5'                                           
                                           FontWeight="Bold" 
                                           FontSize="14"/>
                                <TextBlock x:ConnectionId='6'                                                
                                           TextWrapping="WrapWholeWords"
                                           Margin="0,5,0,0"/>
                                <TextBlock x:ConnectionId='7'                                          
                                           Foreground="#0078D7"
                                           FontWeight="SemiBold"
                                           Margin="0,5,0,10"/>

                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <Button x:ConnectionId='8' Content="Ver ticket"
                                                                      
                                            Padding="10,5"/>
                                    <Button x:ConnectionId='9' Content="Regenerar ticket"
                                                                         
                                            Padding="10,5"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
    </Grid>
</Page>

